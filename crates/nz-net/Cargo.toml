[package]
name = "nz-net"
description = "Network protocol library for nz"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = []
## 启用真系统网卡/配置列举（`if-addrs` + Linux `/proc`；CI 可用，不依赖 root）。
system-inventory = ["dep:if-addrs"]

[dependencies]
thiserror.workspace = true
if-addrs = { workspace = true, optional = true }

[lints]
workspace = true
