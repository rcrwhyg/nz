---
description: 三层产品边界、对照源、后置工具、当前闸禁止事项
alwaysApply: true
---

# 范围

产品：`nz-net`（netwib 能力，非 C API）→ `nz` CLI（netwox 行为 + 工具 0）→ `nz-gui`（netwag 工作流）。

对照源 `netw-ib-ox-ag-5.39.0/` 只读，整树 gitignore，禁止修改或 `git add`。

GUI：仅 native crate（**egui**）。禁止 Tauri / Wry / webview / Tcl-Tk 移植。

后置（实现前单独批准）：工具 73–86、98、101、130–132。清单见 `spec/netaudit/_deferred.md`。

当前闸（crate + CLI 注册表 + 工具 0）：可实现库骨架与工具 0。禁止实现用户工具 1–223 业务、GUI、后置审计。运行时依赖须按决策协议先商量。

对齐 5.39.0 完成前，禁止用「现代化」改变旧工具语义。现代协议只进 `spec/modern/`。

授权：嗅探/伪造/扫描仅本机、实验室或书面授权目标。
