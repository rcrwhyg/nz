---
description: 提交前 review、何时可 amend、对照树永不入库
alwaysApply: true
---

# Git

提交前：

1. 功能做完（测试全绿、零 warning）
2. 请用户 review，可多轮修改，此时不要 commit
3. 用户确认没问题 → 提交恰好一次

提交后，仍停在「这一笔小功能」时：

- 发现问题可以 `--amend`（可多次）
- 仅当：HEAD 就是这一笔、中间没有其他功能提交、且尚未 push

必须新开提交、禁止 amend：

- 这一笔已经 push
- 用户宣告该小功能集完成
- HEAD 之后已经又提交了别的小功能或功能集

提交说明写为什么，前缀例如：`docs(agent):`、`feat(info-001):`、`fix(pkt):`。

禁止：`git add netw-ib-ox-ag-5.39.0/`、`--no-verify`、`--no-gpg-sign`、force push、提交密钥/`.env`。

不改 `git config`。不 push，除非用户明确要求。
