---
description: 第 4 闸起编码硬约束：命名、注释、模块拆分、crate 复用决策、覆盖率
alwaysApply: true
---

# 编码标准（第 4 闸起）

## 命名

标识符、函数、类型、模块必须见名知意：完整英文词，禁止 `buf2`、`do_it`、`tmp` 一类缩写。对齐 netwib/netwox 的概念用语义名（如 `InternetChecksum`），禁止 `netwib_*` / `netwox_*` 符号。

## 注释

每个 public 项必须有 `///`：做什么、对齐哪条对照行为、失败条件。非显然分支写行内注释。禁止无说明的魔法数字。

## 拆分

按 spec 模块拆文件；crate 边界保持 `nz-arg` / `nz-net` → CLI → GUI。禁止为单个工具复制一套私有编解码或私有 argv 解析。一次任务只动一块可验收能力。

参数解析主路径为 **`nz-arg`**（见 `07-nz-arg.mdc`）；遇「能否用 clap」类问题按决策协议处理，默认不引入 clap。

## 已有 crate

遇到成熟 crate 能覆盖本能力时：**立刻停手**，列出 2～3 方案（复用 / 混合 / 自研）、许可证、语义偏差、维护成本，等用户决策。未经确认不引入运行时依赖、不重复造轮子。**例外已拍板**：工具参数解析走自研 `nz-arg`，不以 clap 为主路径。

## 测试

该单元 spec 每条验收都有自动化测试。库代码行覆盖 ≥ 95%；达不到则在对应 spec 写明缺口与原因。**CI 强制**（现阶段 `nz-net`；`nz-arg` 落地后一并强制）；本地 `cargo llvm-cov` 可选，不进 pre-commit。禁止为冲覆盖率改生产行为；禁止无断言空测试。
