# 工具 spec

- 工具号：95
- 标题：TCP remote administration client (get file)
- 建议名：`remadm-get`
- 源：`src/netwox-src/src/tools/000095.c`
- 树节点：client TCP remadm
- 相位：11（remadm）
- 状态：draft

## 行为

从 93 服务端下载 `--file`（远端路径），结果写屏幕或 `-F` 本地文件。

共用见 [_common.md](_common.md)。

## CLI

Usage：`nz 95 -i ip -p port -f remotefile [-F localfile] [-w pass] [-x|+x] [-T sec]`

## 验收

- [ ] `t095_help_parses`
- [ ] `t095_gets_remote_file`

## 非目标

- 不对未授权主机取文件
