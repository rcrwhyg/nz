# 工具 spec

- 工具号：94
- 标题：TCP remote administration client (exec)
- 建议名：`remadm-exec`
- 源：`src/netwox-src/src/tools/000094.c`
- 树节点：client TCP remadm
- 相位：11（remadm）
- 状态：draft

## 行为

连 93 服务端，执行 `--command`，打印输出（或写 `-F` 文件）。`--timeout` **秒**缺省 100。

共用见 [_common.md](_common.md)。

## CLI

Usage：`nz 94 -i ip -p port -c command [-F outfile] [-w pass] [-x|+x] [-T sec]`

| `-c` | `--command` | buf（必填） | `/bin/sh -c ls` |

## 验收

- [ ] `t094_help_parses`
- [ ] `t094_executes_command`
- [ ] `t094_password_default_netwox`

## 非目标

- 不对未授权主机执行
