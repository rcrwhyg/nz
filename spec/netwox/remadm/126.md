# 工具 spec

- 工具号：126
- 标题：HTTP remote administration server
- 建议名：`remadm-http`
- 源：`src/netwox-src/src/tools/000126.c`
- 树节点：server TCP HTTP/remadm
- 相位：11（remadm）
- 状态：draft

## 行为

Web 远程执行面（非静态文件服务 125）。TCP mulser 80；可选 `--login`/`--password`；`--timeout` **毫秒**缺省 60000；`--allowed-clients` 缺省 all。

共用见 [_common.md](_common.md)。

## CLI

Usage：`nz 126 [-P 80] [-l login] [-L pass] [-T ms] [-c ips]`

## 验收

- [ ] `t126_help_parses`
- [ ] `t126_serves_exec_via_http`
- [ ] `t126_login_password_auth`
- [ ] `t126_allowed_clients_reject`

## 依赖的库 spec

[registry.md](../registry.md)

## 非目标

- 不是静态文件服务（125）
- 不对公网开放
