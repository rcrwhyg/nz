# 工具 spec

- 工具号：96
- 标题：TCP remote administration client (put file)
- 建议名：`remadm-put`
- 源：`src/netwox-src/src/tools/000096.c`
- 树节点：client TCP remadm
- 相位：11（remadm）
- 状态：draft

## 行为

上传 `--src-file`（本地）到 93 服务端的 `--dst-file`（远端路径）。

共用见 [_common.md](_common.md)。

## CLI

Usage：`nz 96 -i ip -p port -f localfile -F remotefile [-w pass] [-x|+x] [-T sec]`

## 验收

- [ ] `t096_help_parses`
- [ ] `t096_puts_file`

## 非目标

- 不对未授权主机放文件
