# 工具 spec

- 工具号：185
- 标题：TCP multiclient relay
- 建议名：`tcp-mulrelay`
- HTML：`netw-ib-ox-ag-5.39.0/src/netwox-doc_html/tools/185.html`
- 源：`src/netwox-src/src/tools/185.c`
- 树节点：server + client TCP generic
- 同义词：（无）
- 相位：9
- 状态：draft

## 行为

TCP 多客户中继；`--server-ip` 为 **IP 列表**，每连接随机选一后端。可同时多路。

共用字段见 [_common.md](_common.md)。

## CLI

Usage：`nz 185 [-d device] [-E eth] [-I ip] -P port [-o mixed] [-O mixed] [-t iptype] -x ips -X port [-c ips]`

| `-d` | `--device` | device | （缺省网卡） | 网卡 |
| `-E` | `--src-eth` | eth | HTML 示例 | 源 MAC；触发 VIRT |
| `-I` | `--src-ip` | ip | | 源 IP |
| `-P` | `--src-port` | port（必填） | | 监听端口 |
| `-o` | `--ip4opts` | mixed | | IPv4 options |
| `-O` | `--ip6exts` | mixed | | IPv6 扩展 |
| `-t` | `--iptype` | iptype | `ip4` | 未给 `--src-ip` 时用 |
| `-x` | `--server-ip` | ip / ips | （必填） | 后端地址；185 为列表 |
| `-X` | `--server-port` | port（必填） | | 后端端口 |
| `-c` | `--allowed-clients` | ips | `all` | 允许的客户 IP |

## 例子

- `nz 185 --src-port 8080 --server-ip 192.0.2.2,192.0.2.3 --server-port 80`

## 权限 / 网络

同 183。

## 验收

- [ ] `t185_help_parses`
- [ ] `t185_server_ip_list`
- [ ] `t185_concurrent_relays`
- [ ] `t185_random_backend`

## 依赖的库 spec

[net-sock.md](../../netwib/net-sock.md)、[_common.md](_common.md)、[registry.md](../registry.md)

## 非目标

- 不对未授权目标连接
- 不为该工具复制一套编解码
