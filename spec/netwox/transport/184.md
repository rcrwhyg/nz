# 工具 spec

- 工具号：184
- 标题：UDP relay
- 建议名：`udp-relay`
- HTML：`netw-ib-ox-ag-5.39.0/src/netwox-doc_html/tools/184.html`
- 源：`src/netwox-src/src/tools/184.c`
- 树节点：server + client UDP generic
- 同义词：（无）
- 相位：9
- 状态：draft

## 行为

UDP 中继；首包后查 allowed。其余同 183。

共用字段见 [_common.md](_common.md)。

## CLI

Usage：`nz 184 [-d device] [-E eth] [-I ip] -P port [-o mixed] [-O mixed] [-t iptype] -x ip -X port [-c ips]`

| `-d` | `--device` | device | （缺省网卡） | 网卡 |
| `-E` | `--src-eth` | eth | HTML 示例 | 源 MAC；触发 VIRT |
| `-I` | `--src-ip` | ip | | 源 IP |
| `-P` | `--src-port` | port（必填） | | 监听端口 |
| `-o` | `--ip4opts` | mixed | | IPv4 options |
| `-O` | `--ip6exts` | mixed | | IPv6 扩展 |
| `-t` | `--iptype` | iptype | `ip4` | 未给 `--src-ip` 时用 |
| `-x` | `--server-ip` | ip | （必填） | 后端地址；185 为列表 |
| `-X` | `--server-port` | port（必填） | | 后端端口 |
| `-c` | `--allowed-clients` | ips | `all` | 允许的客户 IP |

## 例子

- `nz 184 --src-port 5353 --server-ip 192.0.2.2 --server-port 53`

## 权限 / 网络

同 183。

## 验收

- [ ] `t184_help_parses`
- [ ] `t184_udp_first_packet_allow`

## 依赖的库 spec

[net-sock.md](../../netwib/net-sock.md)、[_common.md](_common.md)、[registry.md](../registry.md)

## 非目标

- 不对未授权目标连接
- 不为该工具复制一套编解码
