# 工具 spec

- 工具号：218
- 标题：Netwox internal validation suite
- HTML：`netw-ib-ox-ag-5.39.0/src/netwox-doc_html/tools/218.html`
- 源：`src/netwox-src/src/tools/000218.c`
- 树节点：information on netw（原树有；nz Search **不列出**）
- 相位：3
- 状态：draft
- 发布：hidden（见 [registry.md](../registry.md)）

## 行为

原工具跑 netwox 内部自测（asn1、若干哈希/HMAC、SMB 片段等）。nz **不**提供 `nz 218` / 具名子命令。同等能力由 `cargo nextest` 与各库 spec 的验收测试承担，以免清单上「漏工具」。

若实现闸另拍板要保留开发者入口，再改本 spec。

## CLI

无。`nz 218` 必须按未登记失败（与未知号相同）。

## 例子

无用户例。

## 权限 / 网络

无。

## 验收

- [ ] `t218_not_dispatched`：`nz 218` 失败
- [ ] `t218_absent_from_tools_list`：工具 0 `--tools` 不含 218

## 依赖的库 spec

[registry.md](../registry.md)

## 非目标

- 不移植 C 测试框架或 `pktber_test_static` 一类入口名为 `nz 218`
- 不把原自测模块清单当成必须一次性写完的 crate 测试（随相关库 spec 覆盖）
