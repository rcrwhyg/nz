# 工具 spec

- 工具号：180
- 标题：SNTP client obtaining time
- 建议名：`sntp-client`
- 源：`src/netwox-src/src/tools/000180.c`
- 树节点：client UDP SNTP
- 相位：10（SNTP 族）
- 状态：draft

## 行为

UDP 123 请求时间；`--version` 缺省 **3**；显示格式 radio：`--disp-rfc822`（**默认**）/`--disp-unixdate`/`--disp-human`/`--disp-sec`/`--disp-secnsec`。`--timeout` **2000** ms。

## CLI

Usage：`nz 180 -i ip [-p 123] [-V version] [-r|-u|-h|-s|-n] [-T ms]`

| 短 | 长 | 类型 | 默认 |
|----|----|------|------|
| `-V` | `--version` | uint32 | `3` |
| `-r` | `--disp-rfc822` | radio | **选中** |
| `-u` | `--disp-unixdate` | radio | |
| `-h` | `--disp-human` | radio | |
| `-s` | `--disp-sec` | radio | |
| `-n` | `--disp-secnsec` | radio | |
| `-T` | `--timeout` | uint32 | `2000` |

## 验收

- [ ] `t180_help_parses`
- [ ] `t180_rfc822_default`
- [ ] `t180_timeout_2000`

## 非目标

- 不对未授权 NTP 服务器查询
