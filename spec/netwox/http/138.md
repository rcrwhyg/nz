# 工具 spec

- 工具号：138
- 标题：Web spider (use configuration file created by tool 137)
- 建议名：`spider-conf`
- HTML：`netw-ib-ox-ag-5.39.0/src/netwox-doc_html/tools/138.html`
- 源：`src/netwox-src/src/tools/000138.c`
- 树节点：client TCP HTTP spider
- 同义词：curl, wget
- 相位：10（HTTP 族）
- 状态：draft

## 行为

读 137 配置执行 spider 全流水线（run/relink/index/urllist/deltmp）。registry **backspace**。

共用字段见 [_common.md](_common.md)。

## CLI

Usage：`nz 138 -f conffile`

| `-f` | `--conffile` | file_rd（必填） | | 137 生成的配置 |

## 例子

- `nz 138 --conffile spider.conf`（授权实验室）

## 权限 / 网络

仅授权目标。CI 假 HTTP + 临时目录。

## 验收

- [ ] `t138_help_parses`
- [ ] `t138_runs_pipeline`
- [ ] `t138_registry_backspace`

## 依赖的库 spec

[proto-spider.md](../../netwib/proto-spider.md)、[proto-http.md](../../netwib/proto-http.md)、[_common.md](_common.md)、[registry.md](../registry.md)

## 非目标

- 不对未授权目标请求
- 不做 HTTPS/TLS（modern 闸）
- 不对公网 spider
- 不为该工具复制一套 URL/HTTP 栈
