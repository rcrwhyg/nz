# 工具 spec

- 工具号：197
- 标题：WHOIS client guessing server
- 建议名：`whois-guess`
- 源：`src/netwox-src/src/tools/000197.c`
- 树节点：client TCP WHOIS
- 相位：10（WHOIS 族）
- 状态：draft

## 行为

根据 `--query` 猜测 WHOIS 服务器（如按 TLD 路由），然后查询。**无** `--dst-ip`。

## CLI

Usage：`nz 197 -q query [-T ms]`

| `-q` | `--query` | hostname（必填） | |
| `-T` | `--timeout` | uint32 | `60000` |

## 验收

- [ ] `t197_help_parses`
- [ ] `t197_guesses_server_and_queries`

## 非目标

- 不保证 TLD 映射完全正确
