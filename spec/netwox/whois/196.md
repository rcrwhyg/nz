# 工具 spec

- 工具号：196
- 标题：WHOIS client
- 建议名：`whois`
- 源：`src/netwox-src/src/tools/000196.c`
- 树节点：client TCP WHOIS
- 相位：10（WHOIS 族）
- 状态：draft

## 行为

连 `--dst-ip`:`--dst-port`（缺省 43），发 `--query` 并打印响应。

## CLI

Usage：`nz 196 -i ip [-p 43] -q query [-T ms]`

| `-q` | `--query` | hostname（必填） | |
| `-T` | `--timeout` | uint32 | `60000` |

## 验收

- [ ] `t196_help_parses`
- [ ] `t196_queries_whois`

## 非目标

- 不对未授权目标查 WHOIS
