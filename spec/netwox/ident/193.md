# 工具 spec

- 工具号：193
- 标题：IDENT client requesting info about an open session
- 建议名：`ident-query`
- 源：`src/netwox-src/src/tools/000193.c`
- 树节点：client TCP IDENT
- 相位：10（IDENT 族）
- 状态：draft

## 行为

查询 `--localport`/`--remoteport` 的用户身份。

## CLI

Usage：`nz 193 -i ip [-p 113] -L localport -R remoteport [-T ms]`

| `-L` | `--localport` | port（必填） | |
| `-R` | `--remoteport` | port（必填） | |
| `-T` | `--timeout` | uint32 | `60000` |

## 验收

- [ ] `t193_help_parses`
- [ ] `t193_queries_ident`

## 非目标

- 不对未授权主机查 IDENT
