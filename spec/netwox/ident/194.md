# 工具 spec

- 工具号：194
- 标题：IDENT client creating a session and requesting its info
- 建议名：`ident-probe`
- 源：`src/netwox-src/src/tools/000194.c`
- 树节点：client TCP IDENT
- 相位：10（IDENT 族）
- 状态：draft

## 行为

先建 TCP 连接获取本地端口，再查 IDENT。`--remoteport` 必填。

## CLI

Usage：`nz 194 -i ip [-p 113] -R remoteport [-T ms]`

| `-R` | `--remoteport` | port（必填） | |
| `-T` | `--timeout` | uint32 | `60000` |

## 验收

- [ ] `t194_help_parses`
- [ ] `t194_probes_ident`

## 非目标

- 不对未授权主机探测
