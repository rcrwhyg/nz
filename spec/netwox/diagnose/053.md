# 工具 spec

- 工具号：53
- 标题：Ping UDP
- 建议名：`ping-udp`
- HTML：`netw-ib-ox-ag-5.39.0/src/netwox-doc_html/tools/53.html`
- 源：`src/netwox-src/src/tools/000053.c`
- 树节点：ping
- 同义词：（无）
- 相位：8
- 状态：draft

## 行为

UDP 探针。仅端口不可达 ICMP 算 reached。开口无回包不是 bug。`--dst-port` 默认 80。

共用循环与 EthIP 必填见 [_common.md](_common.md)。

## CLI

Usage：`nz 53 -i ip [-p port] [-s spoofip] [-c uint32] [-m uint32] [-b|+b] [-1|+1]`

| 短 | 长 | 类型 | 默认 | 含义 |
|----|----|------|------|------|
| `-i` | `--dst-ip` | ip（必填） | | 目的 |
| `-p` | `--dst-port` | port | `80` | 目的端口 |
| `-s` | `--spoofip` | spoofip（Advanced） | `best` | 发送路径 |
| `-c` | `--max-count` | uint32（Advanced） | `4294967295` | 次数；`0`=无限 |
| `-m` | `--max-ms` | uint32（Advanced） | `1000` | 每次等待 |
| `-b` | `--beep` | bool（Advanced） | 关 | 通了响铃 |
| `-1` | `--display01` | bool（Advanced） | 关 | 只打 `0`/`1` |

## 例子

- `nz 53 --dst-ip 192.0.2.1 --dst-port 1`（文档地址，勿对公网）

## 权限 / 网络

需 sniff+spoof。仅本机 / 实验室 / 书面授权。CI 假通道，不对公网。

## 验收

- [ ] `t053_help_parses`
- [ ] `t053_port_unreach_ok`：注入 port unreachable 出现 `Ok`
- [ ] `t053_open_timeout_no_ok`：无 ICMP 则非 display01 时无 `Ok`

## 依赖的库 spec

[net-sniff.md](../../netwib/net-sniff.md)、[net-spoof.md](../../netwib/net-spoof.md)、[pkt.md](../../netwib/pkt.md)、[net-addr.md](../../netwib/net-addr.md)、[_common.md](_common.md)、[registry.md](../registry.md)

## 非目标

- 不对未授权目标探测
- 不把 UDP 开口无回包当成实现错误
