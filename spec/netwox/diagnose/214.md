# 工具 spec

- 工具号：214
- 标题：Traceroute discovery: graph of network topology
- 建议名：`trace-map`
- HTML：`netw-ib-ox-ag-5.39.0/src/netwox-doc_html/tools/214.html`
- 源：`src/netwox-src/src/tools/000214.c`
- 树节点：traceroute；scan
- 同义词：（无）
- 相位：8
- 状态：draft

## 行为

对 `--ips`（可用 `all`）做 traceroute 并画出远端拓扑。TCP 口视为开、UDP 口视为关。`--icmp` 默认开。`--verbose` 打中间结果，默认关。信息面（跳、开/关）可映射，排版可现代化。

共用循环与 EthIP 必填见 [_common.md](_common.md)。

## CLI

Usage：`nz 214 -i ips [-p ports] [-P ports] [-c|+c] [-s spoofip] [-T uint32] [-t uint32] [-m uint32] [-r|+r] [-v|+v]`

| 短 | 长 | 类型 | 默认 | 含义 |
|----|----|------|------|------|
| `-i` | `--ips` | ips（必填） | 示例 `all` | 目标 |
| `-p` | `--tcpports` | ports | `21,22,23,25,53,79,80,88,110,113,119,139,143,389,443,445,1080,2401,6000` | 视为开的 TCP |
| `-P` | `--udpports` | ports | `1,53,67,68,123,137,138,161,162,177,514` | 视为关的 UDP |
| `-c` | `--icmp` | bool | **开** | 是否扫 ICMP |
| `-s` | `--spoofip` | spoofip（Advanced） | `best` | 发送路径 |
| `-T` | `--min-ttl` | uint32（Advanced） | `1` | 起始 TTL |
| `-t` | `--max-ttl` | uint32（Advanced） | `30` | 最大 TTL |
| `-m` | `--max-ms` | uint32（Advanced） | `1000` | 等待 |
| `-r` | `--resolve` | bool（Advanced） | 关 | 反查 |
| `-v` | `--verbose` | bool（Advanced） | 关 | 中间结果 |

## 例子

- `nz 214 --ips "all"`（文档地址，勿对公网）

## 权限 / 网络

需 sniff+spoof。仅本机 / 实验室 / 书面授权。CI 假通道，不对公网。

## 验收

- [ ] `t214_help_parses`
- [ ] `t214_icmp_default_on`
- [ ] `t214_tcpports_default_contains_80`
- [ ] `t214_graph_has_hop`：假 Time Exceeded 出现在拓扑信息面

## 依赖的库 spec

[net-sniff.md](../../netwib/net-sniff.md)、[net-spoof.md](../../netwib/net-spoof.md)、[pkt.md](../../netwib/pkt.md)、[net-addr.md](../../netwib/net-addr.md)、[_common.md](_common.md)、[registry.md](../registry.md)

## 非目标

- 不对未授权目标探测
- 不把 UDP 开口无回包当成实现错误
